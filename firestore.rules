
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /config/globalSettings {
      allow read, write: if true;
    }
     match /vault/config {
      allow read: if true;
    }
  }
}
